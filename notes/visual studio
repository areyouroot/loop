the visual studio code has nice ui and pre built git features and every thing is easyly understandable by humans
u can directly link the git hub repo or any git repo
it has pre buit git tab so you only need to press that tab to complete that task
the visual studio can clone the repos also 
u can add teams with git in vs

you can go to options to change the themes and twik them and adjeust them 
